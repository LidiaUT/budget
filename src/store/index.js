import { createStore } from 'vuex'
import auth from './auth' // nami sozdannij fail v papke store

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth
  }
})
